__pycache__/
*.pyc

# Credenciais do app da Agenda no Google Cloud. So a versao cifrada
# (credentials.json.gpg) entra no repositorio -- ver pacotes/segredo.sh.
# O texto claro nunca, nem sendo o repositorio privado.
credentials.json
pacotes/**/credentials.json

# Saidas de build
*.deb
release/
